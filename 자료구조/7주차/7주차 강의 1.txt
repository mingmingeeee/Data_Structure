#include <stdio.h>
#include <string.h>
#include <stdlib.h>

// 파일에 가수, 타이틀, 경로명 으로 저장
// 노드와 데이터를 분리하는 이유 : 노래에 대한 다양한 접근이 가능토록 하기 위해

/*
<< 저장 방법 >>
1. artist들을 이니셜에 따라 분류해서 각 그룹을 하나의 단방향 연결리스트로 저장한다.
2. song들을 index를 SIZE로 나눈 나머지가 동일한 것들끼리 분류하여 각 그룹을 하나의 단방향 연결리스트로 저장한다.
SIZE 의 10인 경우의 예 : 72,12,32 등 등 (10으로 나눈 나머지가 2인 경우)
*/

 /*
 main.c : 사용자와 직접 상호작용을 수행하는 함수들
 library.c : 실제로 중요한 기능들을 수행하는 함수들
 srting_tools.c : string 처리와 같은 기능을 수행하는 함수들
 */

/*
macro, tytpe (like struct), 변수, 함수
1. 어떻게 다른 파일에 정의되어 있는 함수를 호출할 수 있는가?
2. 어떻게 다른 파일에 정의되어 있는 변수(external variable)를 사용할 것인가?
3. 어떻게 서로 다른 파일들이 매크로나 타입 정의를 공유할 것인가?
*/

int main() {

}