/*
#include "library.h" -> header에 있는 것들이 그 위에 있다고 간주하는 것이다.
*/

/*
함수의 공유 -> 사실은 하나의 변수를 공유하는 그런 게 아니라 두개의 변수를 따로 쓰는 것

변수의 공유
선언: 컴파일러에게 변수의 존재를 알려 줌
정의: 실제로 메모리를 할당

int i; <- 변수를 선언 그리고 정의 한 것

extern int i; <- i를 선언하는 것 (변수 i라는 것이 어딘가에 있다~~~ 라고만 알려주는 것)
extern int a[]; <- a라는 배열이 어딘가에 있다 ~~~~ 라고만 알려주는 것 알려주기만 하기 때문에 크기를 지정해줄 필요는 없다

extern int i 와 실제 변수 i는 다른 파일에 저장되어 있다.

헤더 파일의 중복 방지

#ifndef -> if not definde OLO 
정의되어있지 않다면 밑을 수행해라
#endif -> ifndef의 끝을 말해줌

*/
